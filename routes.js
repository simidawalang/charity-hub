const routes = require('next-routes')();
// The second paranthesis is used because next-rotes returns a function
// and the paranthesis invokes the function immediately after import or requirement.

module.exports = routes;